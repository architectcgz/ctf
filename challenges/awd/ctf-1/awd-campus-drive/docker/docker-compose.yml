name: ctf

services:
  drive-app:
    image: ctf/awd/awd-campus-drive:latest
    build: .
    labels:
      ctf.project: ctf
      ctf.source: challenge-library
      ctf.challenge.slug: awd-campus-drive
    ports:
      - "18082:8080"
    environment:
      FLAG: "flag{awd_campus_drive_demo}"
      CHECKER_TOKEN: "demo-checker-token"
    volumes:
      - ./app.py:/app/app.py:ro
      - ./challenge_app.py:/app/challenge_app.py:ro
      - ./ctf_runtime.py:/app/ctf_runtime.py:ro
      - drive-data:/data

volumes:
  drive-data:
