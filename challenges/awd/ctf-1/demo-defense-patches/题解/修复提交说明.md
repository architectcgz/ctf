# AWD 三题修复提交说明

这份说明对应 `demo-defense-patches` 目录下的三份修复版代码，目的是让你在演示时可以直接说明：

- 原文件改哪里
- 用哪份修复代码替换
- 每题的关键修复点是什么

## 1. 供应链工单系统

当前题包不是旧版单文件 `docker/app.py` 结构，而是拆成了 Flask 运行入口和业务逻辑两部分：

- `challenges/awd/ctf-1/awd-supply-ticket/docker/runtime/app.py`
- `challenges/awd/ctf-1/awd-supply-ticket/docker/workspace/src/challenge_app.py`

建议提交方式：

- 直接把下面两段代码分别替换到题目容器中的同名文件
- `docker/runtime/ctf_runtime.py` 保持不动，继续保留 `/health` 和 `/api/flag` checker 路径

旧的 `demo-defense-patches/awd-supply-ticket/app.py` 是早期单文件示例，当前这套题包演示请以下面的双文件版本为准。

### 1.1 替换 `docker/runtime/app.py`

```python
import os
import secrets
import sys
from pathlib import Path

from flask import Flask

WORKSPACE_SRC = Path("/workspace/src")
workspace_src = str(WORKSPACE_SRC)
sys.path = [workspace_src] + [path for path in sys.path if path != workspace_src]

from challenge_app import register_challenge_routes
from ctf_runtime import ensure_flag_file, register_runtime_routes

APP = Flask(__name__)
APP.secret_key = os.environ.get("SECRET_KEY") or secrets.token_hex(32)


@APP.before_request
def before_runtime_request():
    ensure_flag_file()


register_runtime_routes(APP)
register_challenge_routes(APP)


if __name__ == "__main__":
    APP.run(host="0.0.0.0", port=8080)
```

### 1.2 替换 `docker/workspace/src/challenge_app.py`

```python
import html
import os
import sqlite3
from pathlib import Path

from flask import g, redirect, render_template_string, request, session, url_for

from ctf_runtime import read_flag

DB_PATH = Path("/workspace/data/ticket.db")
MAX_TITLE_LEN = 80
MAX_CONTENT_LEN = 2000


def db():
    if "db" not in g:
        DB_PATH.parent.mkdir(parents=True, exist_ok=True)
        g.db = sqlite3.connect(DB_PATH)
        g.db.row_factory = sqlite3.Row
    return g.db


def close_db(_error):
    conn = g.pop("db", None)
    if conn is not None:
        conn.close()


def init_business_db():
    conn = db()
    conn.execute(
        "create table if not exists tickets "
        "(id integer primary key autoincrement, title text, content text, status text)"
    )
    conn.execute(
        "create table if not exists users "
        "(username text primary key, password text, role text)"
    )
    admin_password = os.environ.get("ADMIN_PASSWORD")
    if admin_password:
        conn.execute(
            "insert or ignore into users values (?, ?, ?)",
            ("admin", admin_password, "admin"),
        )
    conn.commit()


LAYOUT = """
<!doctype html>
<title>供应链工单系统</title>
<style>
body{font-family:sans-serif;max-width:960px;margin:32px auto;line-height:1.5}
input,textarea{display:block;width:100%;margin:8px 0;padding:8px}
a,button{margin-right:12px}
.ticket{border:1px solid #ddd;padding:12px;margin:12px 0}
</style>
<h1>供应链工单系统</h1>
<p><a href="/">首页</a><a href="/new">创建工单</a><a href="/admin">后台</a><a href="/logout">退出</a></p>
{{ body|safe }}
"""


def page(body):
    return render_template_string(LAYOUT, body=body)


def sanitize_text(value, max_len):
    return (value or "").strip()[:max_len]


def register_challenge_routes(app):
    app.teardown_appcontext(close_db)

    @app.before_request
    def before_request():
        init_business_db()

    @app.get("/")
    def index():
        rows = db().execute("select * from tickets order by id desc").fetchall()
        items = "".join(
            "<div class='ticket'>"
            f"<b>#{r['id']} {html.escape(r['title'])}</b>"
            f"<p>{html.escape(r['content'])}</p>"
            f"<em>{html.escape(r['status'])}</em>"
            "</div>"
            for r in rows
        )
        return page(items or "<p>暂无工单。</p>")

    @app.route("/login", methods=["GET", "POST"])
    def login():
        if request.method == "POST":
            user = db().execute(
                "select * from users where username=? and password=?",
                (request.form["username"], request.form["password"]),
            ).fetchone()
            if user:
                session.clear()
                session["user"] = user["username"]
                session["role"] = user["role"]
                return redirect(url_for("index"))
            return page("<p>登录失败。</p>")
        return page(
            "<form method='post'>"
            "<input name='username' placeholder='username'>"
            "<input name='password' placeholder='password' type='password'>"
            "<button>登录</button></form>"
        )

    @app.get("/logout")
    def logout():
        session.clear()
        return redirect(url_for("index"))

    @app.route("/new", methods=["GET", "POST"])
    def new_ticket():
        if request.method == "POST":
            title = sanitize_text(request.form.get("title"), MAX_TITLE_LEN)
            content = sanitize_text(request.form.get("content"), MAX_CONTENT_LEN)
            if not title or not content:
                return page("<p>工单标题和内容不能为空。</p>"), 400
            db().execute(
                "insert into tickets(title, content, status) values (?, ?, 'open')",
                (title, content),
            )
            db().commit()
            return redirect(url_for("index"))
        return page(
            "<form method='post'>"
            "<input name='title' placeholder='工单标题'>"
            "<textarea name='content' placeholder='问题描述'></textarea>"
            "<button>提交</button></form>"
        )

    @app.get("/notify/<int:ticket_id>")
    def notify(ticket_id):
        row = db().execute("select * from tickets where id=?", (ticket_id,)).fetchone()
        if row is None:
            return page("<p>工单不存在。</p>"), 404
        message = (
            f"通知：工单 #{row['id']} - {html.escape(row['title'])}，"
            f"状态 {html.escape(row['status'])}"
        )
        return page(f"<p>{message}</p>")

    @app.get("/admin")
    def admin():
        if session.get("role") != "admin":
            return redirect(url_for("login"))
        flag = html.escape(read_flag())
        return page(f"<p>管理员面板</p><p>本轮 Flag：<code>{flag}</code></p>")
```

这题主要改了五件事：

1. `SECRET_KEY` 不再使用固定弱值，改成环境变量优先，缺省时随机生成。
2. 默认管理员不再硬编码 `admin123`，只有设置了 `ADMIN_PASSWORD` 才创建管理员。
3. `/notify/<id>` 不再对工单标题做二次 `render_template_string`，直接按普通字符串展示。
4. 登录成功前先 `session.clear()`，避免脏会话沿用。
5. 工单标题、内容和后台输出都增加长度限制与 HTML 转义。

演示时可以重点指这几个位置：

- `runtime/app.py` 里的 `APP.secret_key`
- `init_business_db()`
- `sanitize_text()`
- `new_ticket()`
- `notify()`
- `admin()`

## 2. 校园网盘

原文件位置：

- `challenges/awd/ctf-1/awd-campus-drive/docker/app.py`

修复版位置：

- `challenges/awd/ctf-1/demo-defense-patches/awd-campus-drive/app.py`

建议提交方式：

- 直接用修复版替换原题的 `app.py`

这题主要改了四件事：

1. 上传文件不再保留原始落盘名，统一改成随机文件名。
2. 增加 `BLOCKED_SUFFIXES`，拒绝双扩展脚本类文件。
3. 新增 `META_DIR` 保存“展示名 -> 实际文件”的映射。
4. 预览接口从 `GET /preview?path=...` 改成 `GET /preview/<file_id>`，不再接受原始路径。

演示时可以重点指这几个位置：

- `is_allowed_upload()`
- `upload()`
- `resolve_storage_name()`
- `preview()`

## 3. IoT 设备管理平台

原文件位置：

- `challenges/awd/ctf-1/awd-iot-hub/docker/app.py`

修复版位置：

- `challenges/awd/ctf-1/demo-defense-patches/awd-iot-hub/app.py`

建议提交方式：

- 直接用修复版替换原题的 `app.py`

这题主要改了四件事：

1. 不再使用全局默认设备密钥，改为 `devices` 表按设备存储密钥。
2. `telemetry` 与 `topic_read` 都改成结构化解析 topic，不再做裸前缀判断。
3. `/api/command` 需要 `X-Admin-Token`。
4. `/firmware` 只允许从 `firmware_files` 白名单读取，不直接拼路径。

演示时可以重点指这几个函数：

- `setup()`
- `require_device()`
- `split_topic()`
- `telemetry()`
- `command()`
- `firmware()`

## 4. checker 兼容性

这三份修复版都保留了：

- `GET /health`
- `PUT /api/flag`
- `GET /api/flag`

也就是保留了你当前 AWD 演示赛依赖的 checker 路径，不会因为修补把比赛直接修挂。

## 5. 直接替换映射

如果你现场只想一句话讲明白，可以直接这么说：

- `awd-supply-ticket`：替换 `docker/runtime/app.py` 和 `docker/workspace/src/challenge_app.py`，重点修弱 `SECRET_KEY`、默认口令、模板注入和输出转义。
- `awd-campus-drive`：替换 `docker/app.py`，重点修危险上传和预览路径问题。
- `awd-iot-hub`：替换 `docker/app.py`，重点修默认密钥、Topic ACL 和固件路径读取。
