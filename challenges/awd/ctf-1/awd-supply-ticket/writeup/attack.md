# 攻击思路

## 默认口令

后台默认账号为 `admin / admin123`。未修复时可以直接登录 `/admin` 获取当前实例 Flag。

## 模板注入

`/notify/<id>` 会把工单标题拼进 Jinja2 模板再渲染。创建标题为 `{{ config.__class__ }}` 的工单可验证模板表达式执行。

演示时可以用模板对象链读取 `/flag`。正式比赛中建议由平台动态注入不同 Flag。
