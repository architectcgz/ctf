name: ctf

services:
  ticket-app:
    image: ctf/awd/awd-supply-ticket:latest
    build: .
    labels:
      ctf.project: ctf
      ctf.source: challenge-library
      ctf.challenge.slug: awd-supply-ticket
    ports:
      - "18081:8080"
    environment:
      FLAG: "flag{awd_supply_ticket_demo}"
      ADMIN_PASSWORD: "admin123"
      CHECKER_TOKEN: "demo-checker-token"
    volumes:
      - ./app.py:/app/app.py:ro
      - ./challenge_app.py:/app/challenge_app.py:ro
      - ./ctf_runtime.py:/app/ctf_runtime.py:ro
      - ticket-data:/data

volumes:
  ticket-data:
