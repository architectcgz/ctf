站点使用 HS256 JWT，但签名密钥是弱口令。

## 目标

1. 启动服务。
2. 拿到一份普通用户 token。
3. 伪造管理员 token 并访问管理接口。

## 附件
- `app.py`

## 访问方式
- `默认端口由环境变量 `PORT` 控制，未设置时为 8080。`

## 补充说明
- 本题提供可本地启动的最小 Web 服务。
- 本地测试可直接执行 `python3 app.py`。
