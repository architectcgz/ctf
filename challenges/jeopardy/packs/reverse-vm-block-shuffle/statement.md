附件是一份本地 ELF 和一份外部 block blob。程序不会把完整校验逻辑直接写死在代码里，而是读取 blob 后按块执行。

块顺序被打乱，字段还做了额外掩码。

## 目标

1. 分析二进制，恢复 block blob 的解码方式和状态更新逻辑。
2. 按真实执行顺序还原每个校验块。
3. 逆推出能够通过校验的输入，并把它作为 flag 提交。

## 附件

- `challenge.bin`
- `program.blk`
