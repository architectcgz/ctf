多步骤流程的最终页只看一个可控查询参数，没有真正绑定前置状态。

## 目标

1. 启动服务。
2. 观察最终页的保护条件。
3. 绕过前置步骤直接访问 flag。

## 附件
- `app.py`

## 访问方式
- `默认端口由环境变量 `PORT` 控制，未设置时为 8080。`

## 补充说明
- 本题提供可本地启动的最小 Web 服务。
- 本地测试可直接执行 `python3 app.py`。
