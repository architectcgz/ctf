上传点只检查了第一个扩展名，后续访问路径会把 `.py` 当脚本执行。

## 目标

1. 启动服务。
2. 上传带双扩展的脚本文件。
3. 访问执行结果并拿到 flag。

## 附件
- `app.py`

## 访问方式
- `默认端口由环境变量 `PORT` 控制，未设置时为 8080。`

## 补充说明
- 本题提供可本地启动的最小 Web 服务。
- 本地测试可直接执行 `python3 app.py`。
