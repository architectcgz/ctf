# Pwn 入门：长度门禁

这个容器服务会读取你提交的一段输入。普通输入只能得到长度回显，满足隐藏条件后才会返回 flag。

启动实例后复制平台给出的 TCP 连接命令，用终端连接服务。

## 目标

1. 启动题目服务并使用 `nc <host> <port>` 连接。
2. 发送不同长度的输入，观察回显。
3. 构造能通过门禁的输入。
4. 提交服务返回的 flag。

## 补充说明

- 本题必须启动容器实例。
- 服务是原生 TCP 协议，不是 HTTP 页面。
