恢复接口直接对用户给的 blob 做 `pickle.loads`。

## 目标

1. 启动服务。
2. 构造恶意 pickle 对象。
3. 让服务端反序列化后把 flag 读出来。

## 附件
- `app.py`

## 访问方式
- `默认端口由环境变量 `PORT` 控制，未设置时为 8080。`

## 补充说明
- 本题提供可本地启动的最小 Web 服务。
- 本地测试可直接执行 `python3 app.py`。
