导出接口按对象编号直接取数据，没有做任何所有权校验。

## 目标

1. 启动服务。
2. 观察普通用户能看到的对象编号范围。
3. 改对象编号读取管理员导出并拿到 flag。

## 附件
- `app.py`

## 访问方式
- `默认端口由环境变量 `PORT` 控制，未设置时为 8080。`

## 补充说明
- 本题提供可本地启动的最小 Web 服务。
- 本地测试可直接执行 `python3 app.py`。
