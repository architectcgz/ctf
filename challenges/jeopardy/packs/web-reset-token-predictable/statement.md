重置令牌只依赖可观察的时间戳做伪随机种子。

## 目标

1. 启动服务。
2. 为管理员发起一次重置请求拿到时间戳。
3. 预测 token 并完成确认。

## 附件
- `app.py`

## 访问方式
- `默认端口由环境变量 `PORT` 控制，未设置时为 8080。`

## 补充说明
- 本题提供可本地启动的最小 Web 服务。
- 本地测试可直接执行 `python3 app.py`。
