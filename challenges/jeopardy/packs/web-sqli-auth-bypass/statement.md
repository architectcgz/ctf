这是一个最小登录页，认证逻辑直接把用户输入拼进了 SQL。

## 目标

1. 启动服务。
2. 构造认证绕过 payload。
3. 拿到返回的 flag。

## 附件
- `app.py`

## 访问方式
- `默认端口由环境变量 `PORT` 控制，未设置时为 8080。`

## 补充说明
- 本题提供可本地启动的最小 Web 服务。
- 本地测试可直接执行 `python3 app.py`。
