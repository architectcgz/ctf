站点把权限状态直接放进了可篡改的 JSON Cookie。

## 目标

1. 启动服务。
2. 解码并修改 Cookie 里的角色字段。
3. 访问管理页拿到 flag。

## 附件
- `app.py`

## 访问方式
- `默认端口由环境变量 `PORT` 控制，未设置时为 8080。`

## 补充说明
- 本题提供可本地启动的最小 Web 服务。
- 本地测试可直接执行 `python3 app.py`。
