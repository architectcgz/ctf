导入接口会解析自定义外部实体，并把实体内容直接塞回 XML。

## 目标

1. 启动服务。
2. 构造带外部实体的 XML。
3. 读取本地 flag 文件并提交。

## 附件
- `app.py`

## 访问方式
- `默认端口由环境变量 `PORT` 控制，未设置时为 8080。`

## 补充说明
- 本题提供可本地启动的最小 Web 服务。
- 本地测试可直接执行 `python3 app.py`。
