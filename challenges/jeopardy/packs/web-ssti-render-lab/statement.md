渲染接口会直接求值 `{{ expr }}` 里的表达式。

## 目标

1. 启动服务。
2. 构造模板表达式读取服务端变量。
3. 拿到页面返回的 flag。

## 附件
- `app.py`

## 访问方式
- `默认端口由环境变量 `PORT` 控制，未设置时为 8080。`

## 补充说明
- 本题提供可本地启动的最小 Web 服务。
- 本地测试可直接执行 `python3 app.py`。
