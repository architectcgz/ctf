# Web 入门：门禁请求头

一个很小的内部站点把真正的入口藏在搜索引擎规则里。

启动实例后访问站点，找到门禁页面，并按页面要求补齐请求头拿到 flag。

## 目标

1. 访问首页，确认站点入口。
2. 查看 `robots.txt`，找到隐藏路径。
3. 按门禁页提示添加 HTTP 请求头。
4. 提交页面返回的 flag。

## 补充说明

- 本题必须启动容器实例。
- 可以使用浏览器开发者工具、Burp、curl 或任意 HTTP 客户端。
