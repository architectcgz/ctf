#!/usr/bin/env bash
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

# BEGIN HARNESS ENGINEERING: pre-commit
if [[ -x scripts/run-workflow-stage.sh ]]; then
  bash scripts/run-workflow-stage.sh pre-commit-quick
fi
if [[ -x scripts/check-skill-sync-reminder.sh ]]; then
  bash scripts/check-skill-sync-reminder.sh --staged
fi
# END HARNESS ENGINEERING: pre-commit
