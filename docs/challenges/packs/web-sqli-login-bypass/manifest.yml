spec_version: challenge-pack-v1
slug: web-sqli-login-bypass
title: "SQL 注入：登录绕过"
category: web
difficulty: beginner
tags:
  - "vuln:sqli"
  - "stack:php"
  - "stack:mysql"
  - "kp:auth-bypass"

description:
  file: statement.md

hints:
  - text: "尝试在用户名中输入单引号，观察错误信息"
    cost: 5
  - text: "使用 OR 1=1 可以让 WHERE 条件永远为真"
    cost: 10
  - text: "使用 -- 或 # 可以注释掉后面的 SQL 语句"
    cost: 15

attachments: []

flag:
  mode: dynamic

runtime:
  type: container
  image:
    source: dockerfile
    context_dir: docker
    dockerfile: Dockerfile
  expose:
    - container_port: 80
      protocol: tcp

instance:
  ttl: "2h"
  max_per_user: 1

resources:
  cpu: 0.3
  memory: "128m"
  pids: 50
