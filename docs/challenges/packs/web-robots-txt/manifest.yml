spec_version: challenge-pack-v1
slug: web-robots-txt
title: "robots.txt 信息泄露"
category: web
difficulty: beginner
tags:
  - "kp:recon"
  - "kp:robots"
  - "kp:info-disclosure"

description:
  file: statement.md

hints:
  - text: "检查网站的 robots.txt 文件"
    cost: 5

attachments: []

flag:
  mode: dynamic

runtime:
  type: container
  image:
    source: dockerfile
    context_dir: docker
    dockerfile: Dockerfile
  expose:
    - container_port: 80
      protocol: tcp

instance:
  ttl: "2h"
  max_per_user: 1

resources:
  cpu: 0.2
  memory: "64m"
  pids: 30
