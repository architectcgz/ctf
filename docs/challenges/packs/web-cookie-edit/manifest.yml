spec_version: challenge-pack-v1
slug: web-cookie-edit
title: "Cookie 篡改"
category: web
difficulty: beginner
tags:
  - "vuln:auth-bypass"
  - "kp:cookie"
  - "stack:php"

description:
  file: statement.md

hints:
  - text: "使用浏览器开发者工具查看 Cookie"
    cost: 5
  - text: "尝试修改 role 字段的值"
    cost: 10

attachments: []

flag:
  mode: dynamic

runtime:
  type: container
  image:
    source: dockerfile
    context_dir: docker
    dockerfile: Dockerfile
  expose:
    - container_port: 80
      protocol: tcp

instance:
  ttl: "2h"
  max_per_user: 1

resources:
  cpu: 0.3
  memory: "128m"
  pids: 50
