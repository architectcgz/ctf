spec_version: challenge-pack-v1
slug: web-sqli-union
title: "SQL 注入 UNION 查询"
category: web
difficulty: easy
tags:
  - "vuln:sqli"
  - "kp:union-select"
  - "stack:php"
  - "stack:mysql"
description:
  file: statement.md
hints:
  - text: "使用 UNION SELECT 查询其他表"
    cost: 10
attachments: []
flag:
  mode: dynamic
runtime:
  type: container
  image:
    source: dockerfile
    context_dir: docker
    dockerfile: Dockerfile
  expose:
    - container_port: 80
      protocol: tcp
instance:
  ttl: "2h"
  max_per_user: 1
resources:
  cpu: 0.5
  memory: "256m"
  pids: 100
