spec_version: challenge-pack-v1

slug: web-sqli-login-01
title: "Web-02 SQL Injection: Login Bypass"
category: web
difficulty: easy
tags:
  - vuln:sqli
  - stack:sqlite
  - stack:python
  - stack:flask
  - kp:auth-bypass

description:
  file: statement.md

hints:
  - text: "登录接口把用户输入直接拼进了 SQL。"
    cost: 30
  - text: "SQLite 支持 `-- ` 行注释（注意后面有空格）。"
    cost: 50

attachments: []

flag:
  mode: dynamic # static | dynamic | manual

runtime:
  type: container
  image:
    source: dockerfile # registry | dockerfile | tar
    context_dir: docker
    dockerfile: Dockerfile
  expose:
    - container_port: 8080
      protocol: tcp

instance:
  ttl: "2h"
  max_per_user: 1

resources:
  cpu: 0.5
  memory: "256m"
  pids: 100
