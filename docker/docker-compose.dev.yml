name: ctf

services:
  ctf-awd-defense-ssh-host-key:
    container_name: ctf-awd-defense-ssh-host-key
    image: alpine:3.22
    command:
      - /bin/sh
      - -lc
      - >-
        set -eu;
        mkdir -p /app/storage/runtime;
        if [ ! -s /app/storage/runtime/awd-defense-ssh-host-key.pem ]; then
          apk add --no-cache openssh-keygen >/dev/null;
          ssh-keygen -q -t rsa -b 3072 -N '' -f /app/storage/runtime/awd-defense-ssh-host-key.pem;
          chmod 600 /app/storage/runtime/awd-defense-ssh-host-key.pem;
        fi
    volumes:
      - ${CTF_HOST_ROOT:-${PWD}}/docker/runtime/app-storage:/app/storage

  ctf-frontend:
    container_name: ctf-frontend
    image: ctf-frontend:local
    build:
      context: ../../code/frontend
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:${CTF_FRONTEND_PORT:-5173}:80"
    depends_on:
      ctf-api:
        condition: service_healthy
    healthcheck:
      test:
        - CMD-SHELL
        - wget -qO- http://127.0.0.1/ >/dev/null || exit 1
      interval: 15s
      timeout: 5s
      start_period: 10s
      retries: 10
    restart: unless-stopped
    networks:
      - ctf-network

  ctf-api:
    container_name: ctf-api
    image: ctf-backend:local
    build:
      context: ../../code/backend
      dockerfile: Dockerfile
    env_file:
      - path: infra/registry/ctf-platform-registry.env
        required: false
    environment:
      APP_ENV: dev
      DOCKER_HOST: unix:///var/run/docker.sock
      AWD_CHECKER_ARTIFACT_DIR: ${CTF_HOST_ROOT:-${PWD}}/docker/runtime/awd-checker-artifacts
      CTF_HTTP_HOST: 0.0.0.0
      CTF_HTTP_PORT: 8080
      CTF_CONTAINER_PUBLIC_HOST: 127.0.0.1
      CTF_CONTAINER_ACCESS_HOST: host-gateway.internal
      CTF_CONTEST_AWD_CHECKER_SANDBOX_HOST_WORK_ROOT: ${CTF_HOST_ROOT:-${PWD}}/docker/runtime/checker-sandboxes
      CTF_POSTGRES_HOST: ctf-postgres
      CTF_POSTGRES_PORT: 5432
      CTF_POSTGRES_DATABASE: ctf
      CTF_POSTGRES_USERNAME: postgres
      CTF_POSTGRES_PASSWORD: postgres123456
      CTF_AUTO_MIGRATE: "true"
      CTF_REDIS_ADDR: ctf-redis:6379
      CTF_REDIS_PASSWORD: redis123456
      CTF_REDIS_DB: 2
      CTF_CONTAINER_FLAG_GLOBAL_SECRET: ctf-container-flag-secret-0123456789abcdef
      CTF_CONTAINER_FLAG_GLOBAL_SECRET_FILE: /app/storage/runtime/flag-global-secret
    ports:
      - "127.0.0.1:8080:8080"
    depends_on:
      ctf-postgres:
        condition: service_healthy
      ctf-redis:
        condition: service_healthy
    volumes:
      - ${CTF_HOST_ROOT:-${PWD}}/docker:${CTF_HOST_ROOT:-${PWD}}/docker
      - ${CTF_HOST_ROOT:-${PWD}}/docker/runtime/app-storage:/app/storage
      - /var/run/docker.sock:/var/run/docker.sock
    extra_hosts:
      - "host-gateway.internal:host-gateway"
    healthcheck:
      test:
        - CMD-SHELL
        - wget -qO- http://localhost:8080/ready | grep -q '"status":"ready"' || exit 1
      interval: 15s
      timeout: 5s
      start_period: 30s
      retries: 10
    restart: unless-stopped
    networks:
      - ctf-network

  ctf-awd-defense-ssh-gateway:
    container_name: ctf-awd-defense-ssh-gateway
    image: ctf-backend:local
    build:
      context: ../../code/backend
      dockerfile: Dockerfile
    command:
      - /app/ctf-awd-defense-ssh-gateway
    env_file:
      - path: infra/registry/ctf-platform-registry.env
        required: false
    environment:
      APP_ENV: dev
      DOCKER_HOST: unix:///var/run/docker.sock
      AWD_CHECKER_ARTIFACT_DIR: ${CTF_HOST_ROOT:-${PWD}}/docker/runtime/awd-checker-artifacts
      CTF_HTTP_HOST: 0.0.0.0
      CTF_HTTP_PORT: 8080
      CTF_CONTAINER_PUBLIC_HOST: 127.0.0.1
      CTF_CONTAINER_ACCESS_HOST: host-gateway.internal
      CTF_CONTEST_AWD_CHECKER_SANDBOX_HOST_WORK_ROOT: ${CTF_HOST_ROOT:-${PWD}}/docker/runtime/checker-sandboxes
      CTF_POSTGRES_HOST: ctf-postgres
      CTF_POSTGRES_PORT: 5432
      CTF_POSTGRES_DATABASE: ctf
      CTF_POSTGRES_USERNAME: postgres
      CTF_POSTGRES_PASSWORD: postgres123456
      CTF_AUTO_MIGRATE: "false"
      CTF_REDIS_ADDR: ctf-redis:6379
      CTF_REDIS_PASSWORD: redis123456
      CTF_REDIS_DB: 2
      CTF_CONTAINER_FLAG_GLOBAL_SECRET: ctf-container-flag-secret-0123456789abcdef
      CTF_CONTAINER_FLAG_GLOBAL_SECRET_FILE: /app/storage/runtime/flag-global-secret
    ports:
      - "127.0.0.1:2222:2222"
    depends_on:
      ctf-awd-defense-ssh-host-key:
        condition: service_completed_successfully
      ctf-postgres:
        condition: service_healthy
      ctf-redis:
        condition: service_healthy
    volumes:
      - ${CTF_HOST_ROOT:-${PWD}}/docker:${CTF_HOST_ROOT:-${PWD}}/docker
      - ${CTF_HOST_ROOT:-${PWD}}/docker/runtime/app-storage:/app/storage
      - /var/run/docker.sock:/var/run/docker.sock
    extra_hosts:
      - "host-gateway.internal:host-gateway"
    restart: unless-stopped
    networks:
      - ctf-network

  ctf-postgres:
    container_name: ctf-postgres
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ctf
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123456
    ports:
      - "127.0.0.1:15432:5432"
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U postgres -d ctf
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    volumes:
      - ctf-postgres-data:/var/lib/postgresql/data
    networks:
      - ctf-network

  ctf-redis:
    container_name: ctf-redis
    image: redis:7.4-alpine
    command:
      - redis-server
      - --requirepass
      - redis123456
      - --appendonly
      - "yes"
      - --maxmemory
      - 512mb
      - --maxmemory-policy
      - allkeys-lru
    ports:
      - "127.0.0.1:16379:6379"
    healthcheck:
      test:
        - CMD
        - redis-cli
        - -a
        - redis123456
        - ping
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    volumes:
      - ctf-redis-data:/data
    networks:
      - ctf-network

  ctf-registry:
    profiles:
      - registry
    container_name: ${CTF_REGISTRY_CONTAINER_NAME:-ctf-registry}
    image: ${CTF_REGISTRY_IMAGE:-registry:2}
    environment:
      REGISTRY_AUTH: htpasswd
      REGISTRY_AUTH_HTPASSWD_REALM: ctf-registry
      REGISTRY_AUTH_HTPASSWD_PATH: /auth/htpasswd
    ports:
      - "127.0.0.1:${CTF_REGISTRY_PORT:-5000}:5000"
    restart: ${CTF_REGISTRY_RESTART_POLICY:-always}
    volumes:
      - ${CTF_REGISTRY_DATA_DIR:-${CTF_HOST_ROOT:-${PWD}}/docker/infra/registry/runtime/data}:/var/lib/registry
      - ${CTF_REGISTRY_AUTH_DIR:-${CTF_HOST_ROOT:-${PWD}}/docker/infra/registry/runtime/auth}:/auth:ro
    networks:
      - ctf-network

volumes:
  ctf-postgres-data:
  ctf-redis-data:

networks:
  ctf-network:
    name: ctf-network
    driver: bridge
